// Operador Ternário

const beer = 15;
const ok = beer >= 80 ? true : false;
console.log(ok);

const rock = 'Guitar';
const status = rock === 'Guitar' ? true : false;
console.log(status);

//function arrow
const result = nota => nota >= 7.0 ? true : false;
console.log(result(10.0));